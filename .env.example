GROQ_API_KEY=gsk_your-groq-key-here
# ANTHROPIC_API_KEY=sk-ant-your-key-here  # Optional: only if using anthropic provider
# LinkedIn uses cookies from the file set in config/settings.yaml (no username/password needed)
